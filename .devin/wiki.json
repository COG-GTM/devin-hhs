{
  "version": 1,
  "repo_notes": [
    { "content": "SECURITY: This repository contains NO credentials. All database configuration is managed through Vercel environment variables. Do not document any connection strings, passwords, or environment variable values." }
  ],
  "exclude_paths": [
    "REBUILD-V2-SPEC.md",
    "REBUILD-SPEC.md", 
    "SPEC.md",
    "check_import.sh",
    "*.sh"
  ],
  "pages": [
    {
      "title": "Development Setup",
      "parent": "Development Guide",
      "exclude": true,
      "page_notes": [{ "content": "REMOVED FOR SECURITY. Standard Next.js setup: npm install && npm run dev. Environment variables are configured in Vercel dashboard only." }]
    },
    {
      "title": "Getting Started",
      "parent": "Overview",
      "page_notes": [{ "content": "Standard Next.js project. Run: npm install && npm run dev. No local environment configuration required - all secrets managed via Vercel." }]
    },
    {
      "title": "Database Architecture", 
      "parent": "Architecture",
      "page_notes": [{ "content": "Uses TimescaleDB. Connection configured via Vercel environment variables. No credentials stored in repository." }]
    },
    {
      "title": "Database Module (lib/db.ts)",
      "parent": "Data Layer", 
      "page_notes": [{ "content": "Database client uses process.env.DATABASE_URL configured in Vercel. No hardcoded credentials." }]
    },
    {
      "title": "Environment Configuration",
      "exclude": true,
      "page_notes": [{ "content": "REMOVED. All environment configuration managed via Vercel dashboard." }]
    }
  ]
}
